# 검증 결과

검증일: 2026-09-11. 코드 버전: 1.0.0.

## 자동 테스트

- Java 17: Maven 빌드 및 JUnit/MockBukkit 테스트 **36개 통과**, 실패 0, 오류 0, 건너뜀 0.
- Java 21: 같은 테스트 **36개 통과**, 실패 0, 오류 0, 건너뜀 0.
- 컴파일 대상: Java 17, Paper API 1.20.1, ProtocolLib API 5.3.0.

검증 항목: 동시 토큰 소비, 순간 부하 허용, 제한 크기 상한, 소모 실패 후 회복, 연결별 슬롯 분리, 미완료 로그인 만료, 닫힌 연결 정리, 중복 LOGIN START, 정상 요청 유지, 반복 초과 추방 판정, 위반 이력 만료, 자동 보호 상태의 진입·회복 지연, 설정 검증, 잘못된 reload의 기존 설정 보존, 관찰 모드, 명령 권한, 공통 안내 문구, 수동 추방 문구 보존.

## 실제 서버 검증

테스트 서버는 `127.0.0.1:25585`, `127.0.0.1:25586`에만 바인딩했습니다. 운영 서버에 트래픽을 보내지 않았습니다. ProtocolLib는 Maven API JAR이 아닌 공식 GitHub 릴리스의 설치용 5.3.0 JAR을 사용했습니다.

| 환경 | 확인 결과 |
|---|---|
| Paper 1.20.1 build 196 / Java 17 / ProtocolLib 5.3.0 | 로드 성공, 자체 명령 반복 제한에 의한 추방, 공통 문구 전달, 같은 IP의 정상 유저 유지, 추방된 계정 재접속 성공 |
| 같은 1.20.1 환경 / Paper 기본 SPAM 추방 | 기본 스팸 제한이 먼저 작동해도 공통 문구 전달, 수동/인증 추방 변경 없음은 별도 자동 테스트로 확인 |
| 같은 1.20.1 환경 / 로그인 처리량 제한 | LOGIN 단계 거절 패킷의 공통 문구 전달, 기존 세션 유지 |
| 같은 1.20.1 환경 / online-mode=true, 암호화 응답 대기 | 진행 중 로그인 1개 제한으로 다음 연결 거절, 설정한 10초 제한 시간 이후 첫 연결 종료 및 안내 전달, 슬롯 회복 후 새 연결의 암호화 요청 수신 |
| Paper 1.20.6 build 151 / Java 21 / ProtocolLib 미설치 | 의존 플러그인 없이 로드 성공, 자체 명령 반복 제한 추방, 공통 문구, 같은 IP의 정상 유저 유지, 재접속 성공 |
| Paper 1.20.6 build 151 / Java 21 / ProtocolLib 5.3.0 | 로드 성공, 자체 명령 반복 제한 추방, 공통 문구, 정상 유저 유지, 재접속 성공 |
| 같은 1.20.6 환경 / CUSTOM_PAYLOAD | 패킷 반복 제한 추방, 공통 문구, 정상 유저 유지, 재접속 성공 |

플레이 테스트는 인증 계정 없이 진행하기 위해 외부에서 접근할 수 없는 테스트 서버에서 `online-mode=false`를 사용했습니다. 로그인 대기 시간 검사는 `online-mode=true`에서 암호화 응답을 보내지 않는 테스트 연결로 진행했습니다. **정품 계정의 전체 인증 성공 흐름을 검사한 것은 아닙니다.**

테스트 시 소량의 요청으로 제한을 재현하기 위해 명령 또는 CUSTOM_PAYLOAD를 `per-second: 0.1`, `burst: 1`, 위반 최소 간격을 1초, 추방 기준을 2회로 낮췄습니다. 기본 설정은 더 여유 있는 값입니다. 이 검사는 처리량/공격 규모에 대한 성능 보증이 아닙니다.

`banned-players.json`, `banned-ips.json`은 테스트 후 비어 있음을 확인했습니다. 소스에 밴 API나 밴 명령 실행이 없고, 추방된 유저가 같은 계정으로 재접속하는 동작도 확인했습니다.

## 범위와 한계

- L3/L4 회선 및 TCP 스택 공격은 테스트하거나 해결하지 않습니다. 외부 DDoS 방어망이 필요합니다.
- ProtocolLib는 디코딩된 패킷을 처리합니다. 프레임 디코딩/압축 해제 이전 공격 방어를 검증한 것이 아닙니다.
- Paper 1.20.1과 1.20.6에서 실서버 검증했습니다. 나머지 1.20 계열 패치 버전, 모든 모드·프록시·Geyser 조합을 각각 실행 검증한 것은 아닙니다.
- 같은 IP 검사는 로컬 복수 세션으로 진행했습니다. 실제 PC방·통신사 NAT, 고지연/패킷 손실 환경 전체에 대한 오탐 0% 보증이 아닙니다.
- 다른 플러그인이 추방 이벤트를 취소하거나 서버의 기본 제한/프록시 정책이 먼저 작동하면 최종 동작이 달라질 수 있습니다.
- 자동 테스트/실서버 검증과 별개로 운영 환경의 입장 예산과 요청 제한 수치는 관찰 모드로 측정하여 조정해야 합니다.

## 재현

1. `mvn -B -ntp clean verify`로 자동 테스트와 JAR 빌드를 실행합니다.
2. 실제 서버 검증은 [verification/README.md](verification/README.md)의 로컬 전용 절차와 클라이언트를 사용합니다.
3. 배포 JAR 무결성은 `dist/SHA256SUMS.txt`로 확인합니다.
